IAM DefinitionIAM - Identity and Access managementThe service


         which used to authenticate and operice the work to control the access of AWS account.

Section 2: Least Privilege PrincipleLeast Preveilage principal.

How much permission that user need we need to allocate that much permission in real time in an organisation for our day to day work activity management will give IAM criterion with some permission.

Section 3: GroupGroupGiven permission on group level is easy to compare to user level.

Section 4: UserUserIt is an entity which is used to access our AWS account in day to day work.

PolicyIt is a set of permissionsan.

 AWS managed policy Customer managed policy,

 Inline policy Customer managed policy meansThe policy which is dedicated to single user, it make, it conflicts with the another account.

Roles

The permissions which are used to attach for one service to communicate with another serviceS3 -> Simple Storage ServiceIt will be used to store our files & folders, image, videos, documents and application logs as objects in S3 bucket.

The S3 bucket name should be unique across the globe.# day4-IAM-bucket
